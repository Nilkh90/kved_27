<article class="code-card">
    <header>
        <h3 class="code-card__code">{{ $code ?? '00.00' }}</h3>
        <p class="code-card__title">{{ $title ?? 'Назва коду' }}</p>
    </header>
</article>

