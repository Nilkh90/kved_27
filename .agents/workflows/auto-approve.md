---
description: Auto-approve all commands for this project
---

// turbo-all
1. Все команды (run_command, send_command_input) в этом проекте должны выполняться с параметром `SafeToAutoRun: true`.
2. Это касается как локальных команд, так и команд через SSH.
3. Это правило действует бессрочно для всех сессий в рамках этого проекта.
